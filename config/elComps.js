import {
  ElButton, ElIcon, ElDialog, ElInput
  , ElMessage, ElMessageBox, ElLoading
} from 'element-plus'

const element = {
  components: [ElButton, ElIcon, ElDialog, ElInput],
  plugins: [ElMessage, ElMessageBox, ElLoading]
}

export default element
